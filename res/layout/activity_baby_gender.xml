<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#4BB9D2"
    android:orientation="vertical"
    android:paddingLeft="30dp"
    android:paddingRight="30dp"
    android:paddingTop="55dp" >

    <TextView
        android:id="@+id/baby_gender_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:gravity="center_horizontal"
        android:text="是位？"
        android:textColor="#fff"
        android:textSize="20sp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/baby_gender_title"
        android:layout_centerHorizontal="true"
        android:gravity="center_vertical|center_horizontal"
        android:orientation="vertical" >

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical|center_horizontal" >

            <ToggleButton
                android:id="@+id/boy_toggle_btn"
                style="@style/boyToggleButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
               
                android:background="@drawable/boy_toggle_bg"
                android:checked="true" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical|center_horizontal" >

            <ToggleButton
                android:id="@+id/girl_toggle_btn"
                style="@style/girlToggleButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
            
                android:background="@drawable/girl_toggle_bg" />
        </LinearLayout>
    </LinearLayout>

    <Button
        android:id="@+id/baby_gender_confirm_btn"
         style="@style/RightTextView"
        android:background="@android:color/transparent"
        android:text="确定"
        android:textSize="16sp"
        android:textColor="#fff" />

    <Button
        android:id="@+id/baby_gender_cancel_btn"
        style="@style/LeftTextView"
        android:layout_alignBaseline="@+id/baby_gender_confirm_btn"
        android:layout_alignBottom="@+id/baby_gender_confirm_btn"
        android:layout_toLeftOf="@+id/baby_gender_confirm_btn"
        android:text="上一步"
        />

</RelativeLayout>